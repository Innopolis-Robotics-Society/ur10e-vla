# ur10e-vla

## **Установка и настройка**  

```bash
git clone git@github.com:Innopolis-Robotics-Society/ur10e-vla.git
cd ur10e-vla
docker compose up --build <service>
```

> *note:* service `terminal-amd` is for regular computer on Intel/AMD and `terminal-arm` is for arm cpu like Apple Silicon